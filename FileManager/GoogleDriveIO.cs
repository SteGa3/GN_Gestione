using System;
using Entities;
using RestSharp;





namespace FileManager
{
    public class GoogleDriveIO
    {
        public void GetFile()
        { 
            /*
            var client = new RestSharp.RestClient("http://localhost:52970");
            var request = new RestSharp.RestRequest("/api/Ristorante/{id}", RestSharp.Method.GET);
            request.AddUrlSegment("id", IDRistorante.Text);

            var response = client.Execute<Ristorante>(request);
            var cat = response.Data;
            List<Ristorante> ristoranti = new List<Ristorante>();
            ristoranti.Add(cat);
            */

        }
    }
}
